# PMP + PSPO Mission Control

Static single-page dashboard for tracking PMP and PSPO exam preparation. No build step — `index.html` is fully self-contained (inline CSS/JS).

## Local preview

```
npx serve .
```

## Deploy

Deployed via Vercel. Push to `main` to trigger a deployment, or run:

```
npx vercel --prod
```
